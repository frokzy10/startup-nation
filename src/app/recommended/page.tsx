import React from 'react';
import Typography from "@mui/material/Typography";
import {Container} from "@mui/material";

const RecomentaionPage = () => {
    return (
        <div>
            <Container maxWidth="sm">
                <Typography variant="h3">Our recommended places</Typography>
            </Container>
        </div>
    );
};

export default RecomentaionPage;