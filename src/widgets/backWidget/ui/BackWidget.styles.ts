import {styled} from "styled-components";

export const BackWidgetContainer = styled.div`
  display: flex;
  align-items: center;
  gap: 0;
  height: 20px;
  cursor: pointer;
  margin-bottom: 10px;

  &:hover {
    cursor: pointer;
    opacity: 0.8;
  }
`